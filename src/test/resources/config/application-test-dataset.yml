reconciliation:
  datasets:
    test-dataset:
      source:
        dataSourceRef: source
        query: select name as MigrationKey, name, value from testdata
      target:
        dataSourceRef: target
        query: select name as MigrationKey, name, value from testdata
